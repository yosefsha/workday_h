---
description: Stage all changes, commit with a clean message, and push to remote
---
Stage all changes using `git add -A`. Read through the changes being committed and write a concise, conventional git commit message based on these changes, then execute the commit. After the commit succeeds, push all commits of the current branch to remote.
